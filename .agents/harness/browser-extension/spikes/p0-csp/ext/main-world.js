// Declared with world:'MAIN' — the path VisBug would take instead of a <script> tag.
document.documentElement.dataset.mainWorld = '1';
document.documentElement.dataset.mainWorldCE = String(!!window.customElements);
