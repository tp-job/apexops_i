{
  "build": "browser extension — floating toolbar + connect by project URL, integration branch ext/dev",
  "spec": "build-spec.md",
  "target": ".agents/docs/features/browser-extension.md",
  "features": [
    {
      "id": "P0-01",
      "title": "Decisions X1-X18 locked",
      "priority": "critical",
      "depends_on": [],
      "description": "User said 'make follow plan': every recommendation stands. Spec section 10 records the result and the X10/X17 amendments.",
      "steps": [
        "Spec section 10 lists X1-X18 as locked, with the X10 three-zone table and the X17 vendor amendment"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-19. 8.8 Q3 (Web Store or internal) is still open and does not block P1-P5."
    },
    {
      "id": "P0-02",
      "title": "Auth phase 4 carries the Bearer-path constraint (X3)",
      "priority": "critical",
      "depends_on": [],
      "description": "planning/auth-review-and-restructure-2026-08-25.md says non-web clients keep Bearer whatever phase 4 picks.",
      "steps": [
        "grep 'Constraint added 2026-09-19' in the auth plan under A7"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-19."
    },
    {
      "id": "P0-03",
      "title": "Ingest accepts context (F10)",
      "priority": "high",
      "depends_on": [],
      "description": "context.source = 'extension' needs no schema change.",
      "steps": [
        "schemas/ingest.schema.ts:34 declares context as z.record(z.string(), z.unknown())"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-19 by reading the schema."
    },
    {
      "id": "P0-04",
      "title": "Extension iframe panel survives hostile CSP and is unreadable by the page (R16/R17)",
      "priority": "critical",
      "depends_on": [],
      "description": "spikes/p0-csp: four CSPs, with a hostile page script that patches attachShadow.",
      "steps": [
        "Panel iframe loads under default-src 'none' / frame-src 'none'",
        "Page gets null for iframe.contentDocument and the secret is absent from page HTML",
        "FAILURE CASE: an open shadow root IS read by the page (instrument control)",
        "Closed root made in the ISOLATED world is NOT read, not even through a patched attachShadow"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-19 on Chrome for Testing 131.0.6778.204 and Edge 153.0.4234.32: all four variants pass and the control fails as it should. Firefox is untested and belongs to P7."
    },
    {
      "id": "P0-05",
      "title": "VisBug can be hosted (X17)",
      "priority": "high",
      "depends_on": [],
      "description": "Where VisBug runs, and whether it can be reused.",
      "steps": [
        "Repo status, licence and manifest version checked through the GitHub API",
        "world:'MAIN' content script runs under every CSP variant, and customElements exists there",
        "customElements is null in the ISOLATED world, so <vis-bug> cannot live there"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-19. The repo is ARCHIVED (last push 2026-08-03), Apache-2.0, MV3. Decision: vendor it, do not depend on it. Its script-tag injection was refused on Chrome on every page, including the one with no CSP, by an extension-side policy while use_dynamic_url was on; Edge ran it. We use world:'MAIN' instead."
    },
    {
      "id": "P1-01",
      "title": "One door for session storage",
      "priority": "critical",
      "depends_on": [
        "P0-01"
      ],
      "description": "Every read or write of accessToken / refreshToken / user goes through lib/authSession.ts. build-spec.md lists the 12 sites.",
      "steps": [
        "grep -rn \"localStorage\" app/client/src returns token/user keys ONLY inside the web StorageAdapter",
        "FAILURE CASE: the grep run on main before the change DOES list config.ts, AuthContext.tsx, services/auth.ts and devSessions.ts"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-19. The grep for token/user keys finds nothing in app/client/src. Remaining localStorage calls hold device preferences only (AppLayout assistant panel, ThemeContext, the devSessions list). Control: the same pattern matches api/config.ts on ext/dev. All 12 sites now go through authSession."
    },
    {
      "id": "P1-02",
      "title": "authSession runs on an async StorageAdapter",
      "priority": "critical",
      "depends_on": [
        "P1-01"
      ],
      "description": "StorageAdapter { get, set, remove } returns promises. The web passes a localStorage adapter, and an extension can pass chrome.storage.local.",
      "steps": [
        "Unit test: an in-memory adapter round-trips persist/read/clear",
        "Unit test: an adapter that throws on get/set does not crash the caller, and reads come back as null",
        "npm run build (client) passes — erasableSyntaxOnly"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-19 by authSession.test.ts (16 tests). DESIGN DEVIATION from spec P1 step 2, taken on purpose: the adapter is async, but authSession keeps an in-memory cache, so the getters stay SYNC. initSession() fills the cache before React mounts (main.tsx; .then is a style choice — Vite 7's default target does support top-level await, and an earlier comment claiming otherwise was corrected in P2). adapter.subscribe feeds other contexts' writes into the cache. This avoided pushing await into axios, socket handshakes and first render (R4). Writes update memory synchronously and persistTokens can be awaited; refreshOnce awaits it (R3). The throwing adapter reads as signed out, and the lazy localStorage lookup keeps a blocked store from crashing at startup. npm run build passes."
    },
    {
      "id": "P1-03",
      "title": "Refresh coordination unchanged by the async move",
      "priority": "critical",
      "depends_on": [
        "P1-02"
      ],
      "description": "refreshOnce() still coalesces, still picks up a token another tab already rotated (accessAtStart), and endSession still fires only on 401/403.",
      "steps": [
        "Test: two concurrent callers produce exactly ONE POST /auth/refresh",
        "Test: refresh answers 401 → endSession fires and the tokens are cleared",
        "Test: refresh answers 500 or the network drops → session kept and tokens intact",
        "Test: storage already holds a newer access token than the one at start → no refresh call made"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-19. Tests cover: one refresh for two callers; 401 ends the session; 500 and network failures keep it; adopting another tab's token; waiting for the initial storage read before deciding there is no refresh token. Mutation check: removing each guard (coalescing, await ready, adopt, cache-before-notify) turns exactly its own test red."
    },
    {
      "id": "P1-04",
      "title": "No flash to /login on reload",
      "priority": "high",
      "depends_on": [
        "P1-02"
      ],
      "description": "AuthContext's initial state used a sync localStorage read. With async storage, the first render must stay 'loading' until storage resolves.",
      "steps": [
        "Logged in, reload /dashboard: URL never becomes /login (observe with a MutationObserver or a navigation log, not by eye)",
        "Logged out, open /dashboard: redirected to /login"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-19 in Chrome for Testing 131 on the isolated rig (checks/p1-browser.mjs). Reloading /dashboard records routes ['/dashboard'] only, and signed out lands on /login. There is no loading-state redesign: the cache is filled before render, so the initial state is right from the first frame."
    },
    {
      "id": "P1-05",
      "title": "API base URL is a runtime value",
      "priority": "high",
      "depends_on": [],
      "description": "configureApi({ baseUrl }) replaces the build-time-only read. The web passes VITE_API_URL at startup.",
      "steps": [
        "Web app calls reach the same API as before (network log)",
        "Unit test: configureApi changes the URL a request goes to"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-19. configureApi({baseUrl}) in api/config.ts strips trailing slashes. The test shows it moves the refresh URL. The web keeps the VITE_API_URL default, and the rig ran against :3013 through it."
    },
    {
      "id": "P1-06",
      "title": "P1 browser close-out",
      "priority": "critical",
      "depends_on": [
        "P1-01",
        "P1-02",
        "P1-03",
        "P1-04",
        "P1-05"
      ],
      "description": "Spec P1 exit, observed in a real browser.",
      "steps": [
        "login → reload → still logged in",
        "two tabs, access token expired in both, both fire a request → neither is logged out, and the server sees no reuse alarm",
        "revoke the session from another browser → the next call gets 401 and lands on /login",
        "client CI suite + npm run build + typecheck green"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-19 with checks/p1-browser.mjs on the rig (API :3013, Vite :5199, Chrome for Testing 131): 8/8, then 5/5 on a rerun. In 5 race rounds with both tabs expired, the refreshes were [B:200, A:401] every time, both tabs stayed on /projects, and the token was renewed. Revoke-all from another login sent the next call to /login. FAILURE CASE observed: with the adopt branch removed, BOTH tabs ended at /login (the loser's endSession clears the shared store), so the check detects the regression. Client lint, typecheck (tsc -b), vitest 141/141 and build are clean. RESIDUAL, not reproduced: the loser's 401 could arrive before the winner has persisted (during the claim/issueSession window). The old code had the same exposure, and it was not seen in 5/5 rounds. The extension avoids it by construction, because its service worker is the only refresher (X2)."
    },
    {
      "id": "P2-01",
      "title": "packages/shared workspace exists and CI runs it",
      "priority": "critical",
      "depends_on": [
        "P1-06"
      ],
      "description": "@apexops/shared: TypeScript source, no build of its own; exports ./auth ./api ./types/auth ./sdk-core.",
      "steps": [
        "npm ls @apexops/shared resolves to the workspace link",
        "npm run typecheck/test --workspaces includes it",
        "package-lock.json carries packages/shared"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-20. Root workspaces = packages/shared, app/client, app/server. Lockfile +15 lines. CI needed no new steps (it already runs --workspaces); step names changed from \"both\" to \"all\". React is NOT a dependency: nothing in shared renders yet, so the spec's peerDependency rule has nothing to apply to. It applies the day shared gains a component."
    },
    {
      "id": "P2-02",
      "title": "Session, API client and auth types moved, one import door each",
      "priority": "critical",
      "depends_on": [
        "P2-01"
      ],
      "description": "authSession, api/config + fetchWithAuth, types/auth moved with git mv; client imports @apexops/shared/*; no shims.",
      "steps": [
        "grep finds no @/lib/authSession, @/api/config, @/api/client, @/types/auth in app/client/src",
        "client 189 tests (128 existing + 61 module-load) and shared 29 tests pass",
        "client build + lint clean"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-20 (commit e905f90). getApiBaseUrl has NO default and throws when unconfigured; main.tsx configures it from VITE_API_URL. localStorageAdapter stays in the client, with its own test file."
    },
    {
      "id": "P2-03",
      "title": "No module reads API config or session at load time",
      "priority": "critical",
      "depends_on": [
        "P2-02"
      ],
      "description": "Found by the browser check. services/api.ts had axios.create({ baseURL: getApiBaseUrl() }) at module scope, which runs before main.tsx configures the URL: blank page. tsc, the unit tests and vite build all passed it.",
      "steps": [
        "moduleLoad.test.ts imports all 60 non-UI modules with nothing configured",
        "FAILURE CASE: put the module-scope read back, and the test fails on services/api.ts and its importer"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-20. Fixed by reading baseURL in the request interceptor. The AST scan (every module-scope call to the session/API getters) found exactly that one site. The P1 reload check was ALSO blind to a blank app (\"never visited /login\" is true of an app that never booted). It now requires GET /api/auth/profile to answer 200, shown failing with the bug put back (profile=none)."
    },
    {
      "id": "P2-04",
      "title": "sdk-core split: startCapture(config, transport, host)",
      "priority": "critical",
      "depends_on": [
        "P2-01"
      ],
      "description": "capture.ts ported line for line from v1.js; v1-entry.ts keeps script-tag config + fetch/sendBeacon. Extension inputs: context, mapUrl (optional).",
      "steps": [
        "15 capture tests with a fake window: level patching, page prints first, dedupe count, sampling spares errors, context/mapUrl, internal throw stays inside, no recursion from transport logging, global hooks, unload path, circuit breaker, rejected send, batch halving"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-20 (commit 600aa72). The X9 marker (window.__apexopsSdk) was deliberately NOT added here: P2 promises identical v1 behaviour. It moves to P3."
    },
    {
      "id": "P2-05",
      "title": "/sdk/v1.js is generated and cannot drift, and it behaves exactly as before",
      "priority": "critical",
      "depends_on": [
        "P2-04"
      ],
      "description": "esbuild (es2018, IIFE, unminified) writes app/server/public/sdk/v1.js, which is committed. v1-build.test.ts compares it with a fresh build.",
      "steps": [
        "checks/p2-sdk-parity.mjs: old vs new v1.js on the same page in Chrome, same scenario, all ingest requests diffed",
        "FAILURE CASE: one string changed in the new file → FAIL at the exact line",
        "FAILURE CASE: hand edit to the committed file → drift test fails",
        "checks/sdk-e2e.mjs: generated v1.js on /sdk/test against the real server + DB, plus the regression → alert loop"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-20. Parity: 2 requests (fetch with X-Apexops-Key: 9 events; sendBeacon on unload: 1 event), identical apart from timestamps; same page errors. esbuild absWorkingDir is pinned: output embeds module paths, and a build from the repo root differed from one inside the package. Found when the drift test failed from root. Target es2018 (object spread native); v1 was ES5 by hand, which no supported browser needs. END-TO-END 2026-09-20 (checks/sdk-e2e.mjs, driving the server's own /sdk/test page on the rig, project sprint2-demo): 14/14. The served v1.js is the generated one. The SDK's flush was accepted (202, 3 events). console.error, the uncaught TypeError and the unhandled rejection each landed on their own issue in Postgres (#332-#334). The page's loop ran all 7 steps: ingest → issue → resolve → recur (regressions: 1) → alert fired (unread 1 → 2) → reopened (reopenCount 1). FAILURE CASE: serving a v1.js that cannot read data-project gives 10/14, with no flush and all three counts unchanged. /sdk/demo was not run separately; it loads the same script the same way."
    },
    {
      "id": "P2-06",
      "title": "Found bug fixed: oversized batch dropped its tail",
      "priority": "high",
      "depends_on": [
        "P2-05"
      ],
      "description": "buildBatch halved to fit 64 KB and never re-queued the rest: 40 large events sent 10, lost 30. The comment said the opposite.",
      "steps": [
        "Test failed first (sent 0-9 of 40), passes after the fix",
        "Parity against 600aa72 still identical for the ordinary scenario"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-20 (commit 4a0a93c), its own commit so the port stays a pure move. Remaining limitation, unchanged: an unload flush sends one batch, so a backlog larger than one batch at teardown still loses the rest."
    },
    {
      "id": "P2-07",
      "title": "P2 close-out: web app passes the P1 browser checklist again",
      "priority": "critical",
      "depends_on": [
        "P2-02",
        "P2-03"
      ],
      "description": "Spec P2 exit: the web app passes P1 again after the move.",
      "steps": [
        "checks/p1-browser.mjs on the isolated rig"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-20: 8/8 (5 race rounds). Before the P2-03 fix it was 1/8, which is how the blank page was found. The rig API had to be restarted once: the per-account login throttle returned 429 after many check runs in one day."
    },
    {
      "id": "P3-01",
      "title": "app/extension workspace: WXT, minimal permissions, no static site access",
      "priority": "critical",
      "depends_on": [
        "P2-01"
      ],
      "description": "WXT/MV3 workspace. Content scripts are registered at runtime for bound origins only.",
      "steps": [
        "Production manifest has permissions storage/scripting/alarms/activeTab, optional_host_permissions, and NO host_permissions and NO content_scripts",
        "The e2e build (localhost pre-granted) is a separate --mode and is never the shipped build",
        "npm run build --workspace app/extension is a CI step"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-20. Manifest read after `wxt build`. The content-script entrypoints declare no `matches`, on purpose: WXT copies a runtime script's matches into host_permissions, which would ask for every site at install. WXT 0.21.4 brought one vite (7.3.6) and no second React. `npm audit` totals are identical before and after adding WXT (59, all pre-existing). NOT tested: the real permission prompt. The e2e build pre-grants localhost so a script can bind a site; the prompt itself belongs to P4."
    },
    {
      "id": "P3-02",
      "title": "A bound site's console.error becomes an issue; an unbound site is untouched",
      "priority": "critical",
      "depends_on": [
        "P3-01"
      ],
      "description": "Capture on bound origins only.",
      "steps": [
        "Bound: console.error becomes an issue in Postgres within ~5s",
        "Control: the unbound site has a native console.error and no ApexOps world; the bound site has a patched one and the world"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-20 with checks/p3-extension.mjs on Chrome for Testing 131 AND Edge 153, both 14/14: issue after ~5.3s. Registered scripts are exactly capture(MAIN) + bridge(ISOLATED) with matches = the bound origin only. This check found and drove three server/SDK fixes, see P3-06."
    },
    {
      "id": "P3-03",
      "title": "Privacy: stored URL has no query string or fragment",
      "priority": "critical",
      "depends_on": [
        "P3-02"
      ],
      "description": "X5. Stripped in the page-side capture and again in the worker.",
      "steps": [
        "Open /plain?token=secret123&u=me#frag, fire an error, read the stored event",
        "sanitize.test.ts pins stripUrl"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-20: stored url = http://127.0.0.1:8797/plain. The message and stack are NOT scrubbed, so a token inside an error MESSAGE still reaches the project. That is a known limit of X5 as specified (only the URL is stripped); not addressed."
    },
    {
      "id": "P3-04",
      "title": "Page CSP does not stop delivery",
      "priority": "critical",
      "depends_on": [
        "P3-02"
      ],
      "description": "The page never makes the request: the MAIN world hands the batch to the ISOLATED bridge by DOM event, the bridge to the worker, the worker calls the API.",
      "steps": [
        "Page served with connect-src 'self' delivers, with no CSP violation logged in the page"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-20 on Chrome and Edge. The DOM event (not postMessage) is deliberate: dispatch on pagehide is synchronous across worlds, while a posted message is a task that may never run during teardown. The unload/pagehide path was exercised only by the SDK parity check, not by the extension check."
    },
    {
      "id": "P3-05",
      "title": "X9: a page running its own SDK is not double-counted",
      "priority": "critical",
      "depends_on": [
        "P3-02"
      ],
      "description": "The SDK sets window.__apexopsSdk; the extension's shouldCapture() checks it per event.",
      "steps": [
        "Page with the SDK in its HTML: event count 1",
        "SDK loaded late: before-event once via the extension, after-event once via the SDK",
        "FAILURE CASE: with the guard removed, counts are 2 and 2"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-20. The mutation run gave 12/14: \"counted once\" got count=2 and \"SDK loaded late\" got after=2. Restored and rebuilt afterwards."
    },
    {
      "id": "P3-06",
      "title": "Bugs found by the P3 check, all pre-existing, fixed in their own commits",
      "priority": "high",
      "depends_on": [
        "P3-02"
      ],
      "description": "Nothing had ever loaded /sdk/v1.js or posted to /api/ingest from another origin against this server.",
      "steps": [
        "a603932: /sdk/v1.js was answered by express.static with helmet's Cross-Origin-Resource-Policy: same-origin, so every other site's <script> was blocked (ERR_BLOCKED_BY_RESPONSE.NotSameOrigin). The explicit route moved above static.",
        "949cef9: /api/ingest was mounted below the global CORS and the 100kB JSON parser, so its own CORS and 1MB cap never ran: every cross-origin preflight was refused and a 200kB batch was a 413. Mounted first.",
        "1b1955b: the dedupe count was uncapped; the server rejects count > 10000 with a 400, which the circuit breaker does not count as a failure"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-20, each observed before and after with curl or a real browser: preflight ACAO localhost:5199 became the caller's origin with X-Apexops-Key allowed; a 200kB body went from 413 to reaching key validation (401 for a fake key); 2MB is still 413; app routes still pin CORS to the frontend origin. The two server fixes have NO automated test: they were proven live only. A test needs an HTTP-level harness the server suite does not have."
    },
    {
      "id": "P3-07",
      "title": "The worker's trust boundary: nothing from the page picks the project, key or context",
      "priority": "critical",
      "depends_on": [
        "P3-02"
      ],
      "description": "lib/sanitize.ts + background.ts. The origin comes from sender.origin, never from the message.",
      "steps": [
        "16 unit tests: fields typed and capped to the server schema, context and release REPLACED not merged, a body key ignored, malformed events dropped without throwing, batch capped at 100, oversized body refused",
        "Mutation: trusting the page's context turns the sanitiser test red"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-20. Also mutation-tested: dropping the queue lock and dropping single-flight flush each turn their own test red. In the browser, a throw inside capture (toJSON and toString both throw) does not reach the page. NOT tested in a browser: a hostile page dispatching forged capture events. The unit tests cover what the worker does with a forged body; the DOM-event forging itself was not driven."
    },
    {
      "id": "P3-08",
      "title": "Worker stopped with events queued: none lost, none doubled",
      "priority": "critical",
      "depends_on": [
        "P3-02"
      ],
      "description": "A persisted queue in chrome.storage.session; a 30s alarm retries.",
      "steps": [
        "Server (proxy) down: the batch is in the queue",
        "Worker stopped from chrome:// or edge://serviceworker-internals",
        "Server back, nothing pokes the worker: the event arrives once, from a FRESH worker (in-memory marker gone), and the queue is empty"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-20, Chrome and Edge, 14/14 each. Two harness findings worth keeping. (1) With Puppeteer's DevTools session still attached, a stopped extension worker is NEVER woken again (0 requests in 45s), so the check detaches first. (2) ServiceWorker.stopAllWorkers and Target.closeTarget do not stop an extension worker. Edge renders the stop control as <cr-button data-command=\"stop\">, not <button>: the first Edge run reported the worker \"not stopped\", and its \"arrives once\" pass was therefore meaningless; the marker check exists to catch exactly that. LIMITS by design: delivery is at-least-once (a worker stopped between the 202 and the queue write resends), and storage.session means a full BROWSER restart drops queued events."
    },
    {
      "id": "P3-09",
      "title": "P1-P3 close-out: full CI sequence green",
      "priority": "critical",
      "depends_on": [
        "P3-01",
        "P3-07",
        "P3-08"
      ],
      "description": "The CI steps, run locally.",
      "steps": [
        "typecheck all 4 workspaces",
        "lint",
        "test: shared 32, client 189, server 162, extension 16",
        "build client, server, extension",
        "one React version"
      ],
      "passes": true,
      "notes": "VERIFIED 2026-09-20 (exit 0 on every step). UNEXPLAINED: one test run failed moduleLoad.test.ts for DevRoleSwitcher and useBugTrackerData right after lint/typecheck; the error text was not captured, and 9 reruns (cold cache x4, full-workspace x3, concurrent builds x1, single) all passed. The signature differs from the module-scope-baseURL regression (that one fails services/api.ts + useBugTrackerData). The file's test timeout was widened to 30s as a hedge, NOT a diagnosis. If it recurs, read the message before touching anything."
    },
    {
      "id": "P4",
      "title": "Connect by project URL + binding + web-app card",
      "priority": "critical",
      "depends_on": [
        "P3"
      ],
      "description": "Spec 8.5 P4.",
      "steps": [],
      "passes": false,
      "notes": ""
    },
    {
      "id": "P5a",
      "title": "Rail shell + ApexOps button + iframe panel",
      "priority": "critical",
      "depends_on": [
        "P4"
      ],
      "description": "Spec 9.4 / 10 three-zone model.",
      "steps": [],
      "passes": false,
      "notes": ""
    },
    {
      "id": "P5b",
      "title": "Vendored VisBug tools + Report bug with element",
      "priority": "high",
      "depends_on": [
        "P5a"
      ],
      "description": "Spec 9.4, X17 amended (vendor).",
      "steps": [],
      "passes": false,
      "notes": ""
    },
    {
      "id": "P6",
      "title": "Screenshot",
      "priority": "medium",
      "depends_on": [
        "P5a"
      ],
      "description": "Waits on X7 storage.",
      "steps": [],
      "passes": false,
      "notes": ""
    },
    {
      "id": "P7",
      "title": "Packaging + Firefox",
      "priority": "medium",
      "depends_on": [
        "P5b"
      ],
      "description": "Waits on 8.8 Q3.",
      "steps": [],
      "passes": false,
      "notes": ""
    }
  ]
}
